# kontakt-2026
This is a vibe coded custom frontend for the schedule of pretalx. its main goal was to have icons to differentiate the tracks instead of using just colors.

## Structure

- `index.html`: static document markup
- `styles/fonts.css`: vendored font-face declarations
- `styles/main.css`: application styles
- `scripts/app.js`: schedule loading and UI behavior
